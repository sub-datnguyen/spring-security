# Misc. content of the project
## Git
Copy file `prepare-commit-msg` in `git\prepare-commit-msg` to `<PROJECT_GIT_BASE_DIR>\.git\hooks\` so that we can have the ticket number automatically added to the project.

